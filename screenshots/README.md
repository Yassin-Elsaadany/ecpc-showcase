# Screenshots

Add PNG/JPG files here matching the filenames the main README links to:

- `dashboard.png` — the rack view page with switches / racks visible
- `traffic.png` — the /traffic on-demand snapshot page
- `tooltip.png` — port hover showing "Team X · port Y · UP · 1G"
- `terminal.png` — in-browser SSH terminal with a Cisco prompt
- `whatsapp.png` — a WhatsApp PORT DOWN / PORT RECOVERED message in a hall group
- `mobile.png` — the site on a phone screen with the bottom tab bar

## Before committing publicly

- Blur or replace anything that reveals real infrastructure:
  - Real WhatsApp group names → replace with generic ("Hall Alerts")
  - Real IP addresses if they include a public range (RFC-1918 like 10.x/172.x/192.168.x is fine to keep)
  - Real team / participant numbers → replace with e.g. `1001`, `1042`
  - Any real usernames beyond "yassin"
- Screenshots do not need to match production data — a mock / dev environment is fine.
